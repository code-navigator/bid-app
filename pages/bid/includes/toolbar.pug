v-container.pt-0.pb-0(fluid)
  v-row
    v-col(
      cols="12"
      style="padding: 0px;"
    )
      v-system-bar.d-flex.justify-space-around(
        color="primary"
        window
        dark
        style="padding: 0px;"
      )
        v-btn(
          text
          small
          color="search"
        ) Add
        v-btn(
          text
          small
          color="search"
        ) Remove
